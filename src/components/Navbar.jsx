import React from "react";

const Navbar = () => {
  return (
    <nav className="main-nav">
      <div>PenPages</div>

      <span className="nav-links">
        <span>login</span>
        <span>sign up</span>
      </span>
    </nav>
  );
};

export default Navbar;
